<dev class="container">
  <div class="row">
    <div class='mx-auto'>
      <h3>編集画面</h3>
      <%= form_with model:@item, :url => {:action => :update} do |f| %>
        <%= render 'items_form', f: f %>
        <%= f.submit '変更を保存', class: "btn btn-success" %>
      <% end %>
    </dev>
  </div>
</div>